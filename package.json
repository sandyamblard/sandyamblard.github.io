{
  "name": "cv",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "sass": "sass --watch ./assets/style/style.scss  ./assets/style/style.css",
  
    "prefix" : "postcss ./assets/style/style.css --use autoprefixer -d ./assets/style/prefixed/"
  },
  "dependencies": {
    "core-js": "^3.9.1",
    "mitt": "^2.1.0",
    "nuxt": "^2.15.3"
  }
}
